# ansible-gitops-agent
This is a docker container for a gitops agent. 
It is run on github webhook event and then pulls a git repo and executes an ansible command.

## Quickstart

## Instructions

## Building
Just run `./build.sh`.
Alternatively just build the `Dockerfile`

## Testing
Run `./test.sh`.


